<Window x:Class="SisbackupClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sisbackup" Height="650" Width="900"
        MinHeight="500" MinWidth="750"
        ExtendsContentIntoTitleBar="True"
        SystemBackdrop="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" Padding="16,0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="📦 Sisbackup"
                       FontSize="16" FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="{Binding UserDisplayName}"
                       FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center" Margin="12,0,0,0" />
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="16,0,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Status Card -->
            <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="20" Margin="0,0,0,12"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Status Backup" FontSize="18" FontWeight="Bold" />
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <ProgressBar x:Name="StorageBar" Width="200" Height="6"
                                         Value="46" Maximum="100"
                                         Foreground="{ThemeResource SystemFillColorAccentBrush}" />
                            <TextBlock x:Name="TxtStorage" Text="2.3 GB / 5 GB"
                                       Margin="12,0,0,0" FontSize="13"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                        <TextBlock x:Name="TxtLastSync" Text="Sync terakhir: 2 menit lalu ✅"
                                   FontSize="12" Margin="0,4,0,0"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="BtnSyncNow" Content="🔄 Sync Now"
                                Style="{StaticResource AccentButtonStyle}"
                                Click="BtnSyncNow_Click" Margin="0,0,8,0" />
                        <Button x:Name="BtnPause" Content="⏸ Pause"
                                Click="BtnPause_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Folders & Destinations -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Watch Folders -->
                <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="0"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal"
                                    HorizontalAlignment="Stretch" Padding="16,12">
                            <TextBlock Text="📁 Folder Backup" FontWeight="SemiBold"
                                       VerticalAlignment="Center" />
                            <Button Content="+ Tambah" HorizontalAlignment="Right"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Margin="8,0,0,0" FontSize="12"
                                    Click="BtnAddFolder_Click" />
                        </StackPanel>
                        <ListView x:Name="LstFolders" Grid.Row="1"
                                  Margin="0" BorderThickness="0"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <TextBlock Text="📂" FontSize="16" VerticalAlignment="Center" />
                                        <StackPanel Margin="8,0,0,0">
                                            <TextBlock Text="{Binding Path}" FontSize="13" />
                                            <TextBlock Text="{Binding Status}"
                                                       FontSize="11"
                                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <!-- Destinations -->
                <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8" Padding="0"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal"
                                    HorizontalAlignment="Stretch" Padding="16,12">
                            <TextBlock Text="☁️ Tujuan Backup" FontWeight="SemiBold"
                                       VerticalAlignment="Center" />
                            <Button Content="+ Tambah" HorizontalAlignment="Right"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Margin="8,0,0,0" FontSize="12"
                                    Click="BtnAddDest_Click" />
                        </StackPanel>
                        <ListView x:Name="LstDestinations" Grid.Row="1"
                                  Margin="0" BorderThickness="0"
                                  SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <TextBlock Text="{Binding Icon}" FontSize="16" VerticalAlignment="Center" />
                                        <StackPanel Margin="8,0,0,0">
                                            <TextBlock Text="{Binding Label}" FontSize="13" />
                                            <TextBlock Text="{Binding Detail}"
                                                       FontSize="11"
                                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>

            <!-- Sync Log -->
            <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="16"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="📋 Log Sinkronisasi" FontWeight="SemiBold" />
                    <ListView x:Name="LstLogs" Grid.Row="1" Margin="0,8,0,0"
                              BorderThickness="0" FontSize="12" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
